# Лабораторная работа №06: Паттерны проектирования (RPG Editor)

Проект представляет собой редактор подземелья для игры **Balagur Fate 3**. Программа позволяет создавать NPC, размещать их на карте, сохранять/загружать состояние и моделировать сражения между ними.

Основная цель работы — закрепление принципов SOLID и использование паттернов проектирования: **Factory**, **Visitor**, **Observer**.

## 📋 Описание задания

Необходимо реализовать три типа сущностей:
1.  **Ork** (Орк)
2.  **Willian** (Разбойник)
3.  **Werewolf** (Оборотень)

### Правила сражения
Бой происходит, если NPC находятся на расстоянии меньше или равном заданному (`distance`).
Логика побед (Камень-Ножницы-Бумага):
*   **Орк** убивает **Разбойника**.
*   **Разбойник** убивает **Оборотня**.
*   **Оборотень** убивает **Разбойника**.

*Примечание: Если два NPC убивают друг друга (например, Разбойник и Оборотень при взаимной атаке), погибают оба.*

## 🛠 Используемые паттерны

1.  **Factory Method (Фабрика)**
    *   Используется для создания объектов NPC по их текстовому идентификатору (например, при вводе с консоли или чтении из файла).
    *   Класс: `Factory`.

2.  **Observer (Наблюдатель)**
    *   Используется для логирования событий (убийств).
    *   Реализовано два наблюдателя:
        *   `ConsoleObserver`: выводит сообщения в консоль.
        *   `FileObserver`: записывает сообщения в файл `log.txt`.

3.  **Visitor (Посетитель)**
    *   Используется для реализации логики боя (Double Dispatch).
    *   Класс `CombatVisitor` определяет исход схватки, зная типы атакующего и защищающегося, не нарушая принцип Open-Closed для классов NPC.

## 🚀 Сборка и запуск

### Требования
*   C++ компилятор с поддержкой стандарта **C++17**
*   CMake версии 3.10 или выше
*   Linux / macOS / WSL (Windows)

### Инструкция
1.  Клонируйте репозиторий (или создайте папку с проектом).
2.  Создайте директорию для сборки:
    ```bash
    mkdir build
    cd build
    ```
3.  Сгенерируйте файлы сборки:
    ```bash
    cmake ..
    ```
4.  Соберите проект:
    ```bash
    make
    ```
    *(Или `cmake --build .`)*

## 🎮 Использование

Запустите исполняемый файл:
```bash
./dungeon_editor
